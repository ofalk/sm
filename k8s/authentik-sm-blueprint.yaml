version: 1
metadata:
  name: sm-oidc-setup
entries:

  - model: authentik_providers_oauth2.oauth2provider
    id: sm-provider
    identifiers:
      name: "ServerManager"
    attrs:
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_id: "sm"
      client_secret: "REPLACE_WITH_SECURE_GENERATED_SECRET"
      client_type: "confidential"
      redirect_uris:
        - matching_mode: "strict"
          url: "https://sm.localghost.com/accounts/authentik/login/callback/"
        - matching_mode: "strict"
          url: "https://sm.localghost.com/accounts/oidc/authentik/login/callback/"
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      property_mappings:
        - !Find [authentik_core.propertymapping, [managed, "goauthentik.io/providers/oauth2/scope-openid"]]
        - !Find [authentik_core.propertymapping, [managed, "goauthentik.io/providers/oauth2/scope-profile"]]
        - !Find [authentik_core.propertymapping, [managed, "goauthentik.io/providers/oauth2/scope-email"]]

  - model: authentik_core.application
    id: sm-app
    identifiers:
      slug: "sm"
    attrs:
      name: "ServerManager"
      provider: !KeyOf sm-provider
      group: "Infrastructure"
      meta_launch_url: "https://sm.localghost.com"
